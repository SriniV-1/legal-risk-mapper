# ALRM — Automated Legal Risk Monitor — Dependencies
# Install with: pip install -r requirements.txt

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # required for file uploads in FastAPI
pydantic==2.7.0

# ── NLP / ML (hybrid semantic layer) ─────────────────────────────────────────
# Runs fully locally. No external API calls.
sentence-transformers>=2.7.0  # all-MiniLM-L6-v2 (~80 MB, 384-dim)
spacy>=3.7.0                  # clause segmentation
numpy>=1.24.0                 # cosine sim + embedding math
scikit-learn>=1.4.0           # ML risk classifier (LogisticRegression)
# After installing spacy, also run:
#   python -m spacy download en_core_web_sm

# ── Corpus Pipeline (Phase 1) ────────────────────────────────────────────────
supabase>=2.0.0               # Supabase Python client (wraps postgrest-py)
python-dotenv>=1.0.0          # .env file loading
requests>=2.31.0              # EDGAR scraping + test scripts

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0                 # test runner
httpx>=0.27.0                 # async HTTP client for FastAPI TestClient

# ── Production Hardening ──────────────────────────────────────────────────
slowapi>=0.1.9                # rate limiting for FastAPI (built on `limits`)
gunicorn>=22.0.0              # production WSGI/ASGI server for Railway

# ── LLM providers ─────────────────────────────────────────────────────────────
# Priority: claude-* model → Anthropic; GROQ_API_KEY set → Groq; else → Ollama local
# Groq (recommended free option): set GROQ_API_KEY. Sign up at console.groq.com (no credit card).
#   LRM_EXTRACTION_MODEL=llama-3.1-8b-instant
groq>=0.9.0                   # Groq SDK (free cloud inference, Llama 3.1 8B)
anthropic>=0.25.0             # Anthropic SDK (optional — enables claude-* models)

# ── PDF support ───────────────────────────────────────────────────────────────
pymupdf>=1.24.0               # fast PDF text extraction (MuPDF bindings)
